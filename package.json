{
  "name": "sonar-generic-code-coverage",
  "version": "1.0.0",
  "description": "SonarQube Generic Code Coverage Reporter for Jest/Vitest",
  "main": "./lib/cjs/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "tsc": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "prepublishOnly": "npm run tsc"
  },
  "keywords": [
    "jest",
    "vitest",
    "vitest-reporter",
    "sonar",
    "sonarqube",
    "code-coverage"
  ],
  "author": "Thomas Garry",
  "license": "MIT",
  "devDependencies": {
    "@types/html-escaper": "^3.0.2",
    "@types/istanbul-lib-report": "^3.0.3",
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "html-escaper": "^3.0.3",
    "istanbul-lib-report": "^3.0.1"
  }
}
