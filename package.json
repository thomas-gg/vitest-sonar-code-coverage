{
  "name": "vitest-sonar-code-coverage",
  "version": "1.0.0",
  "description": "SonarQube Code Coverage Reporter for Vitest",
  "main": "./lib/esm/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest",
    "tsc": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "prepublishOnly": "npm run tsc"
  },
  "keywords": [
    "vitest",
    "vitest-reporter",
    "sonar",
    "sonarqube",
    "code-coverage"
  ],
  "author": "Thomas Garry",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.4",
    "@vitest/coverage-v8": "^1.0.4",
    "typescript": "^5.3.3",
    "vite": "^5.0.10",
    "vitest": "^1.0.4"
  },
  "peerDependencies": {
    "vitest": "^1.0.4",
    "@vitest/coverage-v8": "^1.0.4"
  }
}
